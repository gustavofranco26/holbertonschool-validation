name: module3_task1

on:
  push:
    branches: [master]
  schedule:
    - cron: "30 00 * * *"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: "Checkout"
        uses: actions/checkout@v3

      - name: "Setup"
        uses: cd module3_task1 && ./setup.sh

      - name: "Run make build"
        run: cd module3_task1 && make build
